#include "core/random.h"

// Random is header-only via the singleton pattern.
// This .cpp exists only to satisfy the build system.

namespace nf {
// Intentionally empty — all logic is inlined in random.h
}
